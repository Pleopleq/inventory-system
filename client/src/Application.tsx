import React from "react";
import HomePage from "./pages/Home";

const Application: React.FunctionComponent<{}> = (props) => {
  return <HomePage name={"HomePage"}></HomePage>;
};

export default Application;
